# ======================
# APP
# ======================
spring.application.name=TaskScheduler
server.port=8086

# ======================
# PRIMARY DATABASE (JPA)
# ======================
spring.datasource.primary.jdbc-url=jdbc:mysql://localhost:3307/task_scheduler?useSSL=false&serverTimezone=UTC
spring.datasource.primary.username=task_user
spring.datasource.primary.password=C5i32Ld/dc~q
spring.datasource.primary.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================
# SECONDARY DATABASE (READ ONLY - Congregatio)
# ======================
spring.datasource.congregatio.jdbc-url=jdbc:mysql://85.31.60.95:3306/db_congregatio?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.congregatio.username=${CONGREGATIO_DB_USER}
spring.datasource.congregatio.password=${CONGREGATIO_DB_PASS}
spring.datasource.congregatio.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================
# JPA / HIBERNATE (PRIMARY ONLY)
# ======================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# logging SQL (solo si estás debuggeando)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ======================
# THYMELEAF / DEBUG
# ======================
spring.thymeleaf.cache=false
server.error.include-message=always
server.error.include-stacktrace=always
logging.level.org.springframework.web=DEBUG

# ======================
# BUSINESS CONFIG
# ======================
solver.timeLimitSeconds=2

# ======================
# CLOUDINARY
# ======================
cloudinary.cloud-name=dz72vo8eo
cloudinary.api-key=419189526451643
cloudinary.api-secret=${CLOUDINARY_SECRET}

spring.web.resources.add-mappings=true

# ======================
# EMAIL (SMTP Gmail)
# ======================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=escalasoes@arautos.org.br
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.test-connection=false
spring.mail.properties.mail.debug=true